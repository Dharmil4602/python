## User Input
x = input("Enter The First Number: ")
y = input("Enter The Second Number: ")
## We need to specify what type of input we are giving, otherwise it will by default concatenate the input

z = int(x) + int(y)
# z = x + y
print(z)
